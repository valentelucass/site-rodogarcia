{
  "name": "rodogarcia-landing-builder-frontend",
  "private": true,
  "scripts": {
    "dev": "next dev --hostname 127.0.0.1 --port 35112",
    "build": "next build",
    "build:prod": "npm run build && node scripts/prepare-prod-artifact.mjs",
    "start": "next start --hostname 127.0.0.1 --port 35112",
    "start:prod": "node dist-prod/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "next": "16.3.3",
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "typescript": "^5"
  }
}
